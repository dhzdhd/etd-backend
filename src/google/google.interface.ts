export interface Google {
    images: string[];
}

export interface AudioResponse {
    svgs: (string | null)[];
    durations: number[];
    url: string;
}

export interface ImageResponse {
    url: string;
}
